# IME-TRACER
FOR EDUCATIONAL PURPOSES
# Install Python if not already installed
sudo apt update
sudo apt install python3 python3-pip git

# Install dependencies
pip3 install requests phonenumbers colorama

# Run the tool
python3 IME Tracker.py
